// Main exports for the mock system
export {mockClient} from './mock-client.js';
export {
	mockThreads,
	mockMessages,
	mockUsers,
	mockFeed,
	mockPosts,
	generateMessage,
	generateThreads,
} from './mock-data.js';
export type {InstagramClient} from '../client.js';
